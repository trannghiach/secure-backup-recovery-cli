[tool.poetry]
name = "secure-backup-cli"
version = "0.1.0"
description = "Secure Backup CLI"
authors = ["..."]

[tool.poetry.dependencies]
python = "^3.9"
typer = "^0.9.0"
rich = "^13.0.0"
pydantic = "^2.0"
pyyaml = "^6.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"